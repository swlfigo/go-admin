:80 {
	encode gzip

	# API：反向代理到后端容器（compose 服务名 server，保留 /api 前缀）
	handle /api/* {
		reverse_proxy server:8080
	}

	# SPA：静态资源 + 前端路由回退到 index.html
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
